# Core dependencies for the interactive OCR Table Extractor
pandas>=2.0.0
openpyxl>=3.1.0
Pillow>=9.0.0
requests>=2.31.0

# Image processing (if needed for PDF conversion)
pdf2image>=1.16.0

# Additional utilities
python-dotenv>=1.0.0

# PDF & basic image handling
PyMuPDF>=1.24.0
numpy>=1.24.0

# Google Gemini API
google-generativeai>=0.3.0

# Optional (uncomment if you still want local grid visualization)
# opencv-python-headless>=4.9.0.80

# Removed: easyocr, pytesseract (Gemini-only extraction path)
# Removed: tqdm, regex (not required for core Gemini flow)
